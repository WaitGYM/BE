# === Supabase 환경(.env) 예시 ===
# NOTE: 아래 두 값을 Supabase 대시보드에서 복사해 각각 채워 넣으세요.
# Project Settings → Database → Connection strings

# 1) Prisma가 런타임에서 사용하는 '풀러(POOLER, 6543)' 연결 (권장)
# host 예: aws-xxx.pooler.supabase.com:6543
DATABASE_URL="postgresql://postgres:<YOUR-PASSWORD>@aws-<POOL>.pooler.supabase.com:6543/postgres?sslmode=require&pgbouncer=true&connect_timeout=15"

# 2) 마이그레이션 등에 사용하는 '직접(DIRECT, 5432)' 연결
# host 예: db.<PROJECT>.supabase.co:5432
DIRECT_URL="postgresql://postgres:<YOUR-PASSWORD>@db.<PROJECT>.supabase.co:5432/postgres?sslmode=require"

PORT=4000
JWT_SECRET=supersecret_change_me
